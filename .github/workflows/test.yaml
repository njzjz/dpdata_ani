on: push
name: Test
jobs:
  testWithTox:
    name: Test with tox
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-python@v3
      with:
        python-version: '3.10'
    - run: pip install tox
    - run: tox
